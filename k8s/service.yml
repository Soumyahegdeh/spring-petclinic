apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-service
  labels:
    app: spring-petclinic
spec:
  selector:
    app: petclinic
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8080
      nodePort: 30080
  type: NodePort
 